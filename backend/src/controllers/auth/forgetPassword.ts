import type { Request, Response, NextFunction } from "express";

export const forgetPassword = async (req: Request, res: Response) => {
  console.log("🚀 ~ file: forgetPassword.ts:4 ~ forgetPassword ~ req:", req);
};
