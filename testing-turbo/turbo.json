{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "lint": {},
    "build": {
      // "dependsOn": ["^lint"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    // "deploy": ["^lint", "^build"],
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
